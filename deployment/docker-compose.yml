version: '3.8'

services:
  backend:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ..:/app
    environment:
      - FIREBASE_CREDENTIAL_PATH=/app/backend/serviceAccountKey.json
    restart: always
