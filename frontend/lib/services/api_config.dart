class ApiConfig {
  // STRICT LOCAL CONFIGURATION
  // Enforced by Connection Failure Audit
  static const String baseUrl = 'https://cropsense-ai-2ifg.onrender.com'; 
  
  static const Duration timeout = Duration(seconds: 15);
}
